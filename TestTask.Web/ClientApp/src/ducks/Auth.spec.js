import reducer, { SIGN_IN_REQUEST, SIGN_IN_START, SIGN_IN_SUCCESS, SIGN_IN_FAILED, allActions as actions } from './Auth';
import deepFreeze from 'deep-freeze';

describe('reducer', () => {
    it(`stub`, () => expect(1).toBe(1));
});

describe('actions', () => {
});